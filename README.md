# appservR.github.io

This repository is **machine-generated**. Do not edit it directly — changes will be overwritten.

It hosts the built [AppservR documentation website](https://appservR.github.io), published to the
`gh-pages` branch by the [`deploy-docs`](https://github.com/appservR/appservR/blob/main/.github/workflows/deploy-docs.yml)
workflow running in [appservR/appservR](https://github.com/appservR/appservR), where the Hugo source
now lives under `docs/`.

To change the documentation, open a PR against `appservR/appservR` editing files under `docs/`.
